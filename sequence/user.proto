syntax = "proto3";

package sequence;

option go_package = "api/client/sequence;cliseq";

// User module operation number
enum UserSeq {
  UserUnknown = 0;

  // Login
  Login = 1;

  // Push latest user data
  PushSyncUser = 2;

}
